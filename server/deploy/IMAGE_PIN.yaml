version: "0.3.3"
git_ref: "main"
image_tag: "cottage-monitoring:0.3.3"
image_digest: ""
overlay_retired: true
replaces_overlay: "cottage-monitoring:0.3.2"
notes: |
  v0.3.3: set_lights skip only if status AND control already match (stale status no longer swallows OFF).
