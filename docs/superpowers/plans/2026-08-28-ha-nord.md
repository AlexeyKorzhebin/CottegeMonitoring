# Home Assistant on Nord Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Семья видит дом в Home Assistant по этажам и комнатам; свет, ТП, датчики и чайник ходят в тот же каталог Ops, что Telegram, без KNX/MQTT в HA.

**Architecture:** Nord остаётся единственной точкой. Read-Ops получают `area`/`floor`; новый Op `set_auto_heating` и расширенный `set_kettle` живут в том же реестре. HA Container на elion (`--network host`) poll'ит REST-грань раз в 30 с. Мозг дома — LogicMachine. Grafana только SELECT.

**Tech Stack:** Python 3.12 (Nord, pytest, ruff), FastAPI/Pydantic v2, SQLAlchemy 2 async; HA Container (`ghcr.io/home-assistant/home-assistant:stable`), custom component без зависимости `homeassistant` в `server/`; systemd, nginx, certbot.

**Spec:** `docs/superpowers/specs/2026-08-28-ha-nord-design.md`

Спека специально **одна**: HA нельзя выкладывать до живого Nord Ops. Этот план — один поток с воротами после Task 5 (Nord-контракт зелёный) и перед Task 9 (live elion). Не разбивать на два плана.

---

## Global Constraints

- Один процесс Nord: `/mcp` остаётся mount FastAPI. Sidecar MCP запрещён.
- Новое имя Ops только `set_auto_heating`. `set_kettle` — то же имя, расширенные params.
- Каталог после Task 3: **17** имён. Drift: `server/tests/unit/test_ops_drift.py` `SPEC_CATALOG`.
- `house_id` продакшена: `house`. Ключ HA: `--house house --name home-assistant --scopes read,write`.
- Все house-scoped Ops по REST: `POST /api/v1/houses/{house_id}/ops/{name}`. Каталог: `GET /api/v1/ops`.
- Запрещено `@mcp.tool` и ручной FastAPI-роут той же семантики в обход реестра.
- HA не получает KNX, не подписывается на MQTT `cm/#` / `ha/#`, не ставит интеграции KNX/MQTT, `automations.yaml` пустой.
- Grafana не вызывает `command_service` / MQTT.
- `/mcp` в интернет не открывать (в т.ч. на `ha.black-castle.ru`).
- `set_climate` из HA **без** `force_relay`. Реле зоны — только `binary_sensor`.
- Уставку чайника **не** писать в cmd bool `33/1/39`. Слайдер HA рисуется только если `appliance.setpoint_c` не `None`.
- Код Nord на elion не `rsync` / `scp` / `git clone`. Сборка образа локально (`linux/amd64`). Исключение спеки: артефакт HA-компонента (tar) в `/var/lib/homeassistant/custom_components/` — не клон репозитория.
- Alembic `008` на prod-БД **до** рестарта нового образа. Иначе insert команды падает.
- Тег образа сейчас `cottage-monitoring:0.2.9`. После этого плана — `0.3.0` (`pyproject.toml`, `IMAGE_PIN.yaml`, systemd).
- TOOLS.md (issue #4) и MCP SDK 2.x (issue #5) — не этот план.
- `investor-pitch/` не коммитить.
- Коммиты только если пользователь явно просил; иначе оставлять working tree.
- Тесты Nord: `cd server && python -m pytest <path> -v`. Python 3.12.
- Тесты HA-маппера (без пакета `homeassistant`): `cd ha && python -m pytest tests -v`.
- После правок Python Nord: `cd server && ruff check .`

---

## File map

| Path | Role |
|------|------|
| `server/src/cottage_monitoring/services/placement.py` | `floor_from_tags`, `area_from_name`, `placement()` — канон комнат/этажей |
| `server/src/cottage_monitoring/services/agent_actions.py` | Read-Ops + `area`/`floor`; `set_auto_heating`; `set_kettle`/`get_kettle` setpoint |
| `server/src/cottage_monitoring/ops/params.py` | `SetAutoHeatingParams`; `SetKettleParams` (`on` и/или `setpoint_c`) |
| `server/src/cottage_monitoring/ops/catalog.py` | 17-й Op; описания чайника/авто ТП |
| `server/tests/unit/test_placement.py` | Маппинг KNX/Zigbee → `area`/`floor` |
| `server/tests/unit/test_read_ops_placement.py` | Поля в `list_lights` / climate / temp / sensors / kettle |
| `server/tests/unit/test_set_auto_heating.py` | Write GA `1/7/1` |
| `server/tests/unit/test_set_kettle.py` | Params + cmd vs setpoint |
| `server/tests/unit/test_ops_drift.py` | `SPEC_CATALOG` + 17 имён |
| `server/tests/unit/test_ops_cli.py` | Skill/AGENTS содержат `set_auto_heating` |
| `server/tests/unit/test_agent_actions_helpers.py` | `_group_appliances` + `setpoint_c` |
| `skills/cottage-monitoring/SKILL.md` | Авто полы; чайник до N °C |
| `skills/cottage-monitoring/references/openclaw-connection.md` | Probe 17 tools |
| `specs/001-server-mqtt-ingestor/openclaw-cottage-agent-instructions.md` | Канон AGENTS.md |
| `specs/001-server-mqtt-ingestor/research.md` | R-024 placement+ops, R-025 HA Container |
| `specs/001-server-mqtt-ingestor/quickstart.md` | 0.3.0, 17 Ops, HA, alembic 008 |
| `ha/custom_components/cottage_monitoring/` | Custom component (HACS-совместимый manifest) |
| `ha/custom_components/cottage_monitoring/transport.py` | aiohttp transport (только runtime HA) |
| `ha/custom_components/cottage_monitoring/commands.py` | Тела write-Ops без импорта HA |
| `ha/tests/` | Snapshot/client/commands без `homeassistant` |
| `server/deploy/home-assistant.service` | systemd HA `--network host` |
| `server/deploy/nginx/home-assistant.conf` | `ha.black-castle.ru` TLS + WebSocket |
| `server/deploy/ha-sync-component.sh` | tar компонента на volume HA |
| `server/deploy/ha/configuration.yaml` | Канон `http.*` + YAML интеграции |
| `server/deploy/ha/automations.yaml` | `[]` — домашних сцен в HA нет |
| `server/deploy/ha/secrets.yaml.example` | Шаблон ключа HA, не секрет |
| `server/deploy/IMAGE_PIN.yaml` | `0.3.0` |
| `server/deploy/cottage-monitoring.service` | тег образа `0.3.0` |
| `server/pyproject.toml` | `version = "0.3.0"` |

HA-компонент не импортирует пакет `cottage_monitoring`. Словарь комнат живёт только в Nord `placement.py`.

---

### Task 1: Placement — `area` / `floor` из имени и тегов

**Files:**
- Create: `server/src/cottage_monitoring/services/placement.py`
- Test: `server/tests/unit/test_placement.py`

**Interfaces:**
- Produces: `floor_from_tags(tags) -> "1" | "2" | "outside" | None`; `area_from_name(name, tags="") -> str | None`; `placement(*, name, tags) -> dict` с ключами только если значение есть.
- `area` — канон по-русски как в KNX: `кухня`, `гостиная`, `Настина комната`, `Тимнина комната`, `гостевая`, `спальня`, `кабинет`, `тамбур`, `холл`, `ванная`, `крыльцо`, `терраса`, `балкон`. «гостиная 1» / «гостиная 2» → area `гостиная` (два climate в одном area — спека §16).
- Zigbee `zb_sensor_fl1_living_room_temperature` мапится **здесь**, не в HA. Синонимы как у резолвера: зал→гостиная, Настя→Настина комната.

- [ ] **Step 1: Write the failing test**

```python
# server/tests/unit/test_placement.py
from cottage_monitoring.services.placement import area_from_name, floor_from_tags, placement


def test_floor_from_knx_and_zigbee_tags() -> None:
    assert floor_from_tags("1floor,control,light") == "1"
    assert floor_from_tags(["floor1", "temperature", "zb_sensor"]) == "1"
    assert floor_from_tags("2floor,heat,setpoint") == "2"
    assert floor_from_tags(["floor2"]) == "2"
    assert floor_from_tags("control,light,outside") == "outside"
    assert floor_from_tags("outside,weather") == "outside"
    assert floor_from_tags("control,light") is None


def test_area_from_knx_light_name() -> None:
    assert area_from_name("Свет - кухня") == "кухня"
    assert area_from_name("Свет - гостиная") == "гостиная"
    assert area_from_name("Свет - гостиная - торшер") == "гостиная"
    assert area_from_name("Свет - крыльцо", "control,light,outside") == "крыльцо"
    assert area_from_name("Свет - спальня Насти") == "Настина комната"
    assert area_from_name("Уставка ТП - гостиная 1") == "гостиная"
    assert area_from_name("Уставка ТП - гостиная 2") == "гостиная"
    assert area_from_name("Уставка ТП - Настина комната") == "Настина комната"
    assert area_from_name("Уставка ТП - Тимнина комната") == "Тимнина комната"
    assert area_from_name("Темп - кабинет") == "кабинет"


def test_area_from_zigbee_english_name() -> None:
    assert area_from_name("zb_sensor_fl1_kitchen_temperature") == "кухня"
    assert area_from_name("zb_sensor_fl1_living_room_humidity") == "гостиная"
    assert placement(
        name="zb_sensor_fl1_living_room_temperature",
        tags="floor1,temperature,zb_sensor",
    ) == {"floor": "1", "area": "гостиная"}


def test_synonyms_zal_nastya() -> None:
    assert area_from_name("Свет - зал") == "гостиная"
    assert area_from_name("Свет - Настя") == "Настина комната"


def test_placement_omits_missing_keys() -> None:
    assert placement(name="unknown_device_xyz", tags="") == {}
    assert "floor" not in placement(name="Свет - кухня", tags="control,light")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd server && python -m pytest tests/unit/test_placement.py -v`

Expected: FAIL with `ModuleNotFoundError: cottage_monitoring.services.placement`

- [ ] **Step 3: Write minimal implementation**

```python
# server/src/cottage_monitoring/services/placement.py
"""Canonical room/floor labels for read-Ops (HA Areas/Floors consume these)."""

from __future__ import annotations

import re
from typing import Literal

Floor = Literal["1", "2", "outside"]

_ZB_NAME_RE = re.compile(
    r"zb_sensor_fl(?P<fl>[12])_(?P<room>.+)_(?:temperature|humidity|battery)$",
    re.IGNORECASE,
)

_ZB_ROOM: dict[str, str] = {
    "kitchen": "кухня",
    "living_room": "гостиная",
    "livingroom": "гостиная",
    "bedroom": "спальня",
    "guest_room": "гостевая",
    "guest": "гостевая",
    "office": "кабинет",
    "hallway": "холл",
    "hall": "холл",
    "bathroom": "ванная",
    "bath": "ванная",
    "tambour": "тамбур",
    "nastya": "Настина комната",
    "tim": "Тимнина комната",
}

_KNX_PREFIXES = (
    "свет - ",
    "уставка тп - ",
    "темп - ",
    "тп - ",
)

# Longer phrases first.
_NAME_HINTS: tuple[tuple[str, str], ...] = (
    ("настина комната", "Настина комната"),
    ("тимнина комната", "Тимнина комната"),
    ("тимнина", "Тимнина комната"),
    ("настин", "Настина комната"),
    ("спальня насти", "Настина комната"),
    ("гостиная", "гостиная"),
    ("гостев", "гостевая"),
    ("кухн", "кухня"),
    ("спальн", "спальня"),
    ("кабинет", "кабинет"),
    ("тамбур", "тамбур"),
    ("холл", "холл"),
    ("ванн", "ванная"),
    ("крыльц", "крыльцо"),
    ("террас", "терраса"),
    ("балкон", "балкон"),
    ("зал", "гостиная"),
    ("настя", "Настина комната"),
)


def _tagset(tags: list[str] | str) -> set[str]:
    if isinstance(tags, str):
        parts = tags.split(",")
    else:
        parts = tags
    return {t.strip().lower() for t in parts if t and t.strip()}


def floor_from_tags(tags: list[str] | str) -> Floor | None:
    tagset = _tagset(tags)
    if tagset & {"outside", "outdoor", "weather"}:
        return "outside"
    if tagset & {"1floor", "floor1", "fl1"}:
        return "1"
    if tagset & {"2floor", "floor2", "fl2"}:
        return "2"
    return None


def floor_from_name(name: str) -> Floor | None:
    m = _ZB_NAME_RE.search((name or "").strip())
    if m:
        return m.group("fl")  # type: ignore[return-value]
    return None


def _strip_knx_prefix(name: str) -> str:
    n = (name or "").strip()
    low = n.lower().replace("ё", "е")
    for prefix in _KNX_PREFIXES:
        if low.startswith(prefix):
            return n[len(prefix) :].strip()
    if " :status" in low:
        return _strip_knx_prefix(n[: low.index(" :status")])
    return n


def _strip_zone_index(area: str) -> str:
    return re.sub(r"\s+[12]$", "", area).strip()


def area_from_name(name: str, tags: list[str] | str = "") -> str | None:
    _ = tags
    raw = (name or "").strip()
    if not raw:
        return None
    zb = _ZB_NAME_RE.search(raw)
    if zb:
        room_key = zb.group("room").lower()
        if room_key in _ZB_ROOM:
            return _ZB_ROOM[room_key]
    surface = _strip_knx_prefix(raw)
    low = surface.lower().replace("ё", "е")
    for needle, canonical in _NAME_HINTS:
        if needle in low:
            return _strip_zone_index(canonical) if canonical == "гостиная" else canonical
    if low in {"гостиная 1", "гостиная 2"}:
        return "гостиная"
    return None


def placement(*, name: str, tags: list[str] | str) -> dict[str, str]:
    out: dict[str, str] = {}
    floor = floor_from_tags(tags) or floor_from_name(name)
    area = area_from_name(name, tags)
    if floor:
        out["floor"] = floor
    if area:
        out["area"] = area
    return out
```

Если тест «Свет - зал» не матчится из-за порядка hints — поправить `_NAME_HINTS`, не хардкодить исключения в HA.

- [ ] **Step 4: Run test to verify it passes**

Run: `cd server && python -m pytest tests/unit/test_placement.py -v && ruff check src/cottage_monitoring/services/placement.py tests/unit/test_placement.py`

Expected: PASS

- [ ] **Step 5: Commit** (только если пользователь просил)

```bash
git add server/src/cottage_monitoring/services/placement.py server/tests/unit/test_placement.py
git commit -m "$(cat <<'EOF'
feat: add Nord placement labels for HA areas and floors

EOF
)"
```

---

### Task 2: Поля `area` / `floor` в read-Ops

**Files:**
- Modify: `server/src/cottage_monitoring/services/agent_actions.py`
- Test: `server/tests/unit/test_read_ops_placement.py`

**Где появляются поля (спека §9):** `list_lights.items[]`, `get_climate.zones[]` (рядом с `room`; `area` = канон комнаты, `floor` с тегов уставки), `get_temperature.items[]`, `get_sensors.items[]`, `get_kettle` (на корне ответа и/или в `appliance`). Лишние ключи JSON; MCP не ломается. Ресурсный `GET /objects` для раскладки HA не использовать.

Хелпер в `agent_actions.py`:

```python
from cottage_monitoring.services.placement import placement

def _with_placement(item: dict, *, name: str, tags: list[str] | str) -> dict:
    item.update(placement(name=name, tags=tags))
    return item
```

`list_lights`: у `c` (ResolvedObject) есть `.name` и `.tags`.

`get_temperatures` / `get_sensors`: то же на каждом item.

`get_climate`: для зоны `place = placement(name=sp.name, tags=sp.tags)`; `area = place.get("area")`; `floor = place.get("floor")`; поле `room` оставить как сейчас (`room_query` с «гостиная 1»).

`get_kettle`: после `_group_appliances` вызвать `placement` по `appliance["name"]` и тегам первого object; если area нет — **не** подставлять «кухня» в Nord (спека: иначе поле не ставим). HA-маппер (Task 6) для чайника может fallback на `кухня`.

- [ ] **Step 1: Write the failing test**

```python
# server/tests/unit/test_read_ops_placement.py
from __future__ import annotations

import asyncio
from unittest.mock import MagicMock

from cottage_monitoring.services import agent_actions
from cottage_monitoring.services.object_resolver import ObjectRole, ResolvedObject, ResolveResult


def _ro(ga: str, name: str, tags: list[str], role: ObjectRole) -> ResolvedObject:
    return ResolvedObject(ga=ga, name=name, tags=tags, role=role, datatype=1)


def test_list_lights_items_include_area_and_floor(monkeypatch) -> None:
    kitchen = _ro("1/1/7", "Свет - кухня", ["1floor", "control", "light"], ObjectRole.LIGHT_CONTROL)

    async def fake_resolve(*_a, **_k):
        return ResolveResult(status="ok", matches=[kitchen])

    async def fake_states(*_a, **_k):
        return {"1/1/7": True}

    async def fake_status(*_a, **_k):
        return {}

    monkeypatch.setattr(agent_actions, "resolve_objects", fake_resolve)
    monkeypatch.setattr(agent_actions, "_get_state_map", fake_states)
    monkeypatch.setattr(agent_actions, "_light_status_by_control_name", fake_status)

    out = asyncio.run(agent_actions.list_lights(MagicMock(), "house"))
    item = out["items"][0]
    assert item["area"] == "кухня"
    assert item["floor"] == "1"
    assert item["name"] == "Свет - кухня"
    assert "ga" in item


def test_get_climate_zone_has_area_floor_and_keeps_room(monkeypatch) -> None:
    sp = _ro(
        "1/6/5",
        "Уставка ТП - гостиная 1",
        ["1floor", "heat", "setpoint", "temp"],
        ObjectRole.CLIMATE_SETPOINT,
    )

    async def fake_resolve(_session, _house_id, *, query=None, role=None, **_k):
        if role == ObjectRole.CLIMATE_SETPOINT:
            return ResolveResult(status="ok", matches=[sp])
        return ResolveResult(status="not_found", matches=[])

    async def fake_states(*_a, **_k):
        return {"1/6/5": 23, "1/7/1": True}

    monkeypatch.setattr(agent_actions, "resolve_objects", fake_resolve)
    monkeypatch.setattr(agent_actions, "_get_state_map", fake_states)

    out = asyncio.run(agent_actions.get_climate(MagicMock(), "house"))
    zone = out["zones"][0]
    assert zone["room"] == "гостиная 1"
    assert zone["area"] == "гостиная"
    assert zone["floor"] == "1"


def test_get_temperature_zigbee_maps_english_name(monkeypatch) -> None:
    sensor = _ro(
        "33/1/13",
        "zb_sensor_fl1_kitchen_temperature",
        ["floor1", "temperature", "zb_sensor"],
        ObjectRole.ROOM_TEMP,
    )

    async def fake_resolve(_session, _house_id, *, role=None, query=None, kind=None, **_k):
        if role == ObjectRole.ROOM_TEMP:
            return ResolveResult(status="ok", matches=[sensor])
        return ResolveResult(status="ok", matches=[])

    async def fake_states(*_a, **_k):
        return {"33/1/13": 21.5}

    monkeypatch.setattr(agent_actions, "resolve_objects", fake_resolve)
    monkeypatch.setattr(agent_actions, "_get_state_map", fake_states)

    out = asyncio.run(agent_actions.get_temperatures(MagicMock(), "house", query="кухня"))
    item = next(i for i in out["items"] if i["ga"] == "33/1/13")
    assert item["area"] == "кухня"
    assert item["floor"] == "1"
    assert item["source"] == "air"


def test_get_sensors_humidity_has_placement(monkeypatch) -> None:
    hum = _ro(
        "33/1/14",
        "zb_sensor_fl1_kitchen_humidity",
        ["floor1", "humidity", "zb_sensor"],
        ObjectRole.ROOM_HUMIDITY,
    )

    async def fake_resolve(*_a, **_k):
        return ResolveResult(status="ok", matches=[hum])

    async def fake_states(*_a, **_k):
        return {"33/1/14": 45}

    monkeypatch.setattr(agent_actions, "resolve_objects", fake_resolve)
    monkeypatch.setattr(agent_actions, "_get_state_map", fake_states)

    out = asyncio.run(agent_actions.get_sensors(MagicMock(), "house", kind="humidity"))
    assert out["items"][0]["area"] == "кухня"
    assert out["items"][0]["floor"] == "1"


def test_get_kettle_forwards_placement_when_classifiable(monkeypatch) -> None:
    from types import SimpleNamespace

    matches = [
        SimpleNamespace(
            ga="33/1/37",
            name="ble_teapot_RK-M173S_temp",
            tags=["ble", "teapot", "temp"],
            role=ObjectRole.ZIGBEE_APPLIANCE,
        ),
    ]

    async def fake_resolve(*_a, **_k):
        return ResolveResult(status="ok", matches=matches)  # type: ignore[arg-type]

    async def fake_states(*_a, **_k):
        return {"33/1/37": 54}

    monkeypatch.setattr(agent_actions, "resolve_objects", fake_resolve)
    monkeypatch.setattr(agent_actions, "_get_state_map", fake_states)

    out = asyncio.run(agent_actions.get_kettle(MagicMock(), "house"))
    appliance = out["appliance"]
    assert "setpoint_c" in appliance or appliance.get("setpoint_c") is None
    # teapot English name has no room token — area may be absent
    assert "area" not in appliance or appliance["area"] in (None, "кухня")
```

Последний тест: после Task 2 `area` у чайника **отсутствует** (нет токена комнаты). Не ставить кухню в Nord. Поле `setpoint_c` появится в Task 4 — здесь достаточно `appliance` dict.

- [ ] **Step 2: Run test to verify it fails**

Run: `cd server && python -m pytest tests/unit/test_read_ops_placement.py -v`

Expected: FAIL (`area` KeyError / assertion)

- [ ] **Step 3: Write minimal implementation**

В `list_lights` цикл:

```python
        row = {"name": c.name, "ga": c.ga, "value": val, "on": _as_on(val)}
        items.append(_with_placement(row, name=c.name, tags=c.tags))
```

В `get_temperatures` оба места, где собирается item — `_with_placement(..., name=obj.name, tags=obj.tags)`.

В `get_sensors` — то же.

В `get_climate` после сборки dict зоны:

```python
        place = placement(name=sp.name, tags=sp.tags)
        zone = {
            "room": room_query,
            "setpoint_ga": sp.ga,
            "setpoint": states.get(sp.ga),
            "floor_temp": ...,
            "room_temp": ...,
            "relay_on": ...,
        }
        zone.update(place)
        zones.append(zone)
```

В `get_kettle` после выбора единственного appliance:

```python
        first = result.matches[0] if result.matches else None
        if first is not None:
            appliances[0].update(placement(name=first.name, tags=first.tags))
            appliances[0].update(placement(name=appliances[0]["name"], tags=first.tags))
```

Не удалять существующие ключи (`ga`, `room`, `on`).

- [ ] **Step 4: Run test to verify it passes**

Run: `cd server && python -m pytest tests/unit/test_read_ops_placement.py tests/unit/test_placement.py tests/unit/test_set_lights.py tests/unit/test_agent_actions_helpers.py -v && ruff check src/cottage_monitoring/services/agent_actions.py`

Expected: PASS (старые тесты света/чайника не сломаны)

- [ ] **Step 5: Commit** (только если пользователь просил)

```bash
git add server/src/cottage_monitoring/services/agent_actions.py server/tests/unit/test_read_ops_placement.py
git commit -m "$(cat <<'EOF'
feat: attach area and floor to Nord read-Ops

EOF
)"
```

---

### Task 3: Op `set_auto_heating` — каталог 17 имён

**Files:**
- Modify: `server/src/cottage_monitoring/ops/params.py`
- Modify: `server/src/cottage_monitoring/ops/catalog.py` (вставить **после** `set_climate`, до `get_energy_status`)
- Modify: `server/src/cottage_monitoring/services/agent_actions.py`
- Modify: `server/tests/unit/test_ops_drift.py` (`SPEC_CATALOG`)
- Test: `server/tests/unit/test_set_auto_heating.py`

**Контракт:** permission `write`, house_scoped, body `{on: bool}` → `_resolve_device_and_send(..., AUTO_HEATING_GA, on, comment="mcp set_auto_heating")`. Read по-прежнему `get_climate.auto_heating_enabled`. Отдельного `get_auto_heating` нет.

- [ ] **Step 1: Write the failing tests**

В `test_ops_drift.py` в `SPEC_CATALOG` после `"set_climate"`:

```python
    "set_auto_heating": ("write", True),
```

```python
# server/tests/unit/test_set_auto_heating.py
from __future__ import annotations

import asyncio

from fastapi import HTTPException
import pytest

from cottage_monitoring.ops.catalog import load_catalog
from cottage_monitoring.ops.params import SetAutoHeatingParams, validate_params
from cottage_monitoring.ops.registry import registry
from cottage_monitoring.services import agent_actions
from cottage_monitoring.services.object_resolver import AUTO_HEATING_GA


def test_set_auto_heating_params_require_on() -> None:
    load_catalog()
    spec = registry.get("set_auto_heating")
    with pytest.raises(HTTPException) as ei:
        validate_params(spec, {})
    assert ei.value.status_code == 422
    parsed = SetAutoHeatingParams(on=False)
    assert parsed.on is False


def test_set_auto_heating_writes_1_7_1(monkeypatch) -> None:
    sent: list[tuple] = []

    async def fake_send(session, house_id, ga, value, *, comment=None):
        sent.append((house_id, ga, value, comment))
        return {"request_id": "r1", "ga": ga, "value": value, "status": "sent"}

    monkeypatch.setattr(agent_actions, "_resolve_device_and_send", fake_send)
    out = asyncio.run(agent_actions.set_auto_heating(None, "house", on=False))
    assert sent[0][1] == AUTO_HEATING_GA
    assert sent[0][1] == "1/7/1"
    assert sent[0][2] is False
    assert "set_auto_heating" in (sent[0][3] or "")
    assert out["status"] == "sent"
```

В `test_ops_drift.py::test_mcp_tool_schema_keeps_existing_arguments` добавить:

```python
    assert set(tools["set_auto_heating"].inputSchema["properties"]) == {"on", "house_id"}
    assert set(tools["set_auto_heating"].inputSchema["required"]) == {"on"}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd server && python -m pytest tests/unit/test_set_auto_heating.py tests/unit/test_ops_drift.py -v`

Expected: FAIL (`SetAutoHeatingParams` / catalog name missing / SPEC_CATALOG mismatch)

- [ ] **Step 3: Write minimal implementation**

`params.py`:

```python
class SetAutoHeatingParams(OpParams):
    on: bool
```

`agent_actions.py` рядом с `get_climate`:

```python
async def set_auto_heating(
    session: AsyncSession,
    house_id: str,
    *,
    on: bool,
) -> dict[str, Any]:
    return await _resolve_device_and_send(
        session, house_id, AUTO_HEATING_GA, on, comment="mcp set_auto_heating"
    )
```

`catalog.py` — `_op("set_auto_heating", "write", "... GA 1/7/1. Turning off lets LM Lua drop all floor relays. Distinct from per-zone relay status.", _handler(agent_actions, "set_auto_heating"), params_model=params.SetAutoHeatingParams)`.

`AUTO_HEATING_GA` уже импортирован в `agent_actions`.

- [ ] **Step 4: Run test to verify it passes**

Run: `cd server && python -m pytest tests/unit/test_set_auto_heating.py tests/unit/test_ops_drift.py tests/unit/test_ops_cli.py tests/integration/test_ops_rest.py -v && ruff check src/cottage_monitoring/ops tests/unit/test_set_auto_heating.py tests/unit/test_ops_drift.py`

Expected: PASS, `GET /ops` содержит 17 имён для write-ключа

- [ ] **Step 5: Commit** (только если пользователь просил)

```bash
git add server/src/cottage_monitoring/ops/params.py server/src/cottage_monitoring/ops/catalog.py server/src/cottage_monitoring/services/agent_actions.py server/tests/unit/test_set_auto_heating.py server/tests/unit/test_ops_drift.py
git commit -m "$(cat <<'EOF'
feat: add set_auto_heating Op for underfloor auto algorithm

EOF
)"
```

---

### Task 4: Чайник — `setpoint_c` в `get_kettle` / `set_kettle`

**Files:**
- Modify: `server/src/cottage_monitoring/ops/params.py` (`SetKettleParams`)
- Modify: `server/src/cottage_monitoring/services/agent_actions.py` (`_group_appliances`, `set_kettle`)
- Modify: `server/src/cottage_monitoring/ops/catalog.py` (описание `set_kettle` / `get_kettle`)
- Modify: `server/tests/unit/test_agent_actions_helpers.py`
- Modify: `server/tests/unit/test_ops_drift.py` (схема MCP `set_kettle`)
- Test: `server/tests/unit/test_set_kettle.py`

**Контракт params:** `on: bool | None = None`, `setpoint_c: float | None = None`; хотя бы одно; `setpoint_c` в диапазоне 40–100 включительно. `on=false` — выкл cmd. `on=true` без уставки — кипятить (cmd bool, как сейчас). `setpoint_c` без `on` — только запись объекта уставки. **Никогда** не писать °C в `33/1/39`.

`_group_appliances`: ключи `setpoint_ga`, `setpoint_c` (может быть `None`). Классификация setpoint: `"setpoint" in name.lower()` или `"setpoint" in tags`, **до** ветки `temp` (чтобы `heat_temp`/`*_setpoint` не попали в `temp`).

Если объекта уставки нет — `setpoint_c` ключ есть, значение `None`. `set_kettle(setpoint_c=80)` → HTTP 404 `Kettle setpoint object not found`.

- [ ] **Step 1: Write the failing tests**

Расширить `test_group_appliances_teapot_summary`: `assert g.get("setpoint_c") is None` (после реализации ключ обязателен).

Новый тест с объектом уставки:

```python
def test_group_appliances_reads_setpoint_not_as_temp() -> None:
    matches = [
        SimpleNamespace(ga="33/1/39", name="ble_teapot_RK-M173S_cmd", role=ObjectRole.ZIGBEE_APPLIANCE, tags=["ble", "control", "zigbee_send"]),
        SimpleNamespace(ga="33/1/37", name="ble_teapot_RK-M173S_temp", role=ObjectRole.ZIGBEE_APPLIANCE, tags=["ble", "teapot", "temp"]),
        SimpleNamespace(ga="33/1/40", name="ble_teapot_RK-M173S_setpoint", role=ObjectRole.ZIGBEE_APPLIANCE, tags=["ble", "teapot", "setpoint"]),
        SimpleNamespace(ga="33/1/38", name="ble_teapot_RK-M173S_state", role=ObjectRole.ZIGBEE_APPLIANCE, tags=["ble", "teapot", "status"]),
    ]
    groups = _group_appliances(matches, {"33/1/39": True, "33/1/37": 54, "33/1/40": 80, "33/1/38": True})
    assert groups[0]["temp"] == 54
    assert groups[0]["setpoint_c"] == 80
    assert groups[0]["setpoint_ga"] == "33/1/40"
    assert groups[0]["temp_ga"] == "33/1/37"
```

```python
# server/tests/unit/test_set_kettle.py
from __future__ import annotations

import asyncio

from fastapi import HTTPException
import pytest

from cottage_monitoring.ops.catalog import load_catalog
from cottage_monitoring.ops.params import SetKettleParams, validate_params
from cottage_monitoring.ops.registry import registry
from cottage_monitoring.services import agent_actions
from cottage_monitoring.services.object_resolver import ObjectRole, ResolvedObject, ResolveResult


def test_set_kettle_params_require_on_or_setpoint() -> None:
    load_catalog()
    spec = registry.get("set_kettle")
    with pytest.raises(HTTPException) as ei:
        validate_params(spec, {})
    assert ei.value.status_code == 422
    with pytest.raises(HTTPException):
        validate_params(spec, {"setpoint_c": 20})
    assert validate_params(spec, {"on": True}) == {"on": True, "setpoint_c": None}
    assert validate_params(spec, {"setpoint_c": 80})["setpoint_c"] == 80.0


def test_set_kettle_on_writes_cmd_ga(monkeypatch) -> None:
    cmd = ResolvedObject(
        ga="33/1/39",
        name="ble_teapot_RK-M173S_cmd",
        tags=["ble", "control", "zigbee_send"],
        role=ObjectRole.ZIGBEE_APPLIANCE,
        datatype=1,
    )

    async def fake_resolve(*_a, **_k):
        return ResolveResult(status="ok", matches=[cmd])

    sent: list[tuple] = []

    async def fake_send(session, house_id, ga, value, *, comment=None):
        sent.append((ga, value))
        return {"request_id": "r1", "ga": ga, "value": value, "status": "sent"}

    monkeypatch.setattr(agent_actions, "resolve_objects", fake_resolve)
    monkeypatch.setattr(agent_actions, "_resolve_device_and_send", fake_send)

    asyncio.run(agent_actions.set_kettle(None, "house", on=True, setpoint_c=None))
    assert sent == [("33/1/39", True)]


def test_set_kettle_setpoint_does_not_write_cmd_bool(monkeypatch) -> None:
    sp = ResolvedObject(
        ga="33/1/40",
        name="ble_teapot_RK-M173S_setpoint",
        tags=["ble", "teapot", "setpoint"],
        role=ObjectRole.ZIGBEE_APPLIANCE,
        datatype=9,
    )

    async def fake_resolve(*_a, **_k):
        return ResolveResult(status="ok", matches=[sp])

    sent: list[tuple] = []

    async def fake_send(session, house_id, ga, value, *, comment=None):
        sent.append((ga, value))
        return {"request_id": "r1", "ga": ga, "value": value, "status": "sent"}

    monkeypatch.setattr(agent_actions, "resolve_objects", fake_resolve)
    monkeypatch.setattr(agent_actions, "_resolve_device_and_send", fake_send)

    asyncio.run(agent_actions.set_kettle(None, "house", on=None, setpoint_c=80))
    assert sent == [("33/1/40", 80)]
    assert all(ga != "33/1/39" for ga, _ in sent)


def test_set_kettle_setpoint_404_when_object_missing(monkeypatch) -> None:
    cmd = ResolvedObject(
        ga="33/1/39",
        name="ble_teapot_RK-M173S_cmd",
        tags=["ble", "control", "zigbee_send"],
        role=ObjectRole.ZIGBEE_APPLIANCE,
        datatype=1,
    )

    async def fake_resolve(*_a, **_k):
        return ResolveResult(status="ok", matches=[cmd])

    monkeypatch.setattr(agent_actions, "resolve_objects", fake_resolve)

    with pytest.raises(HTTPException) as ei:
        asyncio.run(agent_actions.set_kettle(None, "house", on=None, setpoint_c=80))
    assert ei.value.status_code == 404
    assert "setpoint" in str(ei.value.detail).lower()
```

В `test_mcp_tool_schema_keeps_existing_arguments`:

```python
    kettle_props = set(tools["set_kettle"].inputSchema["properties"])
    assert kettle_props == {"on", "setpoint_c", "house_id"}
    kettle_req = set(tools["set_kettle"].inputSchema["required"])
    assert "house_id" not in kettle_req
    assert kettle_req <= {"on", "setpoint_c"}
```

Pydantic v2 с двумя optional без required в JSON Schema — `required` может быть `[]`. Главное: `on` больше не единственный required. Старый вызов `{on: true}` должен проходить `validate_params`.

- [ ] **Step 2: Run test to verify it fails**

Run: `cd server && python -m pytest tests/unit/test_set_kettle.py tests/unit/test_agent_actions_helpers.py tests/unit/test_ops_drift.py::test_mcp_tool_schema_keeps_existing_arguments -v`

Expected: FAIL (`SetKettleParams` still `on: bool`)

- [ ] **Step 3: Write minimal implementation**

```python
from pydantic import model_validator

class SetKettleParams(OpParams):
    on: bool | None = None
    setpoint_c: float | None = None

    @model_validator(mode="after")
    def on_or_setpoint(self) -> SetKettleParams:
        if self.on is None and self.setpoint_c is None:
            raise ValueError("either on or setpoint_c is required")
        if self.setpoint_c is not None and not (40 <= self.setpoint_c <= 100):
            raise ValueError("setpoint_c must be between 40 and 100")
        return self
```

`_group_appliances` — в `setdefault` добавить `"setpoint_ga": None, "setpoint_c": None`. В цикле, **перед** temp:

```python
        if "setpoint" in n or "setpoint" in tags:
            g["setpoint_ga"] = m.ga
            g["setpoint_c"] = val
        elif "cmd" in n or (...existing cmd...):
            ...
        elif "temp" in n or "temp" in tags:
            ...
```

`set_kettle` сигнатура `*, on: bool | None = None, setpoint_c: float | None = None`. Логика:

1. Собрать matches тем же циклом query `teapot` / `ble_teapot` / `чайник` / `kettle`, что сейчас.
2. Если `setpoint_c is not None`: найти единственный match с setpoint в имени или тегах; иначе 404; `await _resolve_device_and_send(..., ga, setpoint_c, comment="mcp set_kettle setpoint")`.
3. Если `on is not None`: существующий путь cmd (`cmd` in name или tags control+zigbee_send), fallback GA `33/1/39`.
4. Вернуть dict с ключами выполненных шагов (`setpoint` / cmd fields). Не писать setpoint в cmd.

Описание в catalog: `set_kettle` — `on` and/or `setpoint_c` (40–100); never write °C to cmd 33/1/39. `get_kettle` — `appliance.temp` current, `appliance.setpoint_c` when object exists.

- [ ] **Step 4: Run test to verify it passes**

Run: `cd server && python -m pytest tests/unit/test_set_kettle.py tests/unit/test_agent_actions_helpers.py tests/unit/test_ops_drift.py tests/unit/test_read_ops_placement.py -v && ruff check src/cottage_monitoring/ops/params.py src/cottage_monitoring/services/agent_actions.py`

Expected: PASS

- [ ] **Step 5: Объект уставки на LogicMachine (эта спека, не follow-up)**

На elion (туннель к prod PG, как `./server/scripts/tunnel-start.sh`):

```bash
# из server/, с DB_URL prod через туннель
python - <<'PY'
import os, asyncio
from sqlalchemy import text
from sqlalchemy.ext.asyncio import create_async_engine
async def main():
    e = create_async_engine(os.environ["DB_URL"])
    async with e.connect() as c:
        rows = (await c.execute(text(
            "SELECT ga, name, tags FROM objects "
            "WHERE house_id='house' AND is_active "
            "AND (name ILIKE '%teapot%' OR tags ILIKE '%teapot%' OR tags ILIKE '%kettle%') "
            "ORDER BY ga"
        ))).all()
        for r in rows:
            print(r)
    await e.dispose()
asyncio.run(main())
PY
```

Ожидание сейчас: только `33/1/37` temp, `33/1/38` state, `33/1/39` cmd. Объекта `*setpoint*` нет.

На LM (Mosaic / объекты, LAN контроллера):

1. Свободный GA в той же линии Zigbee/BLE: проверить что `33/1/40` не занят (если занят — следующий свободный в `33/1/*`).
2. Создать объект: имя `ble_teapot_RK-M173S_setpoint`, теги `ble,teapot,setpoint`, datatype **float temperature** (как другие °C, не bool), **writable**.
3. Найти resident/скрипт чайника (имя содержит `teapot` / `RK-M173S` / `Redmond`). Добавить: запись в этот GA → BLE-команда «нагрев до N °C» вендора; чтение текущей уставки обратно в GA если протокол отдаёт. **Не** слать число в `33/1/39`.
4. Дождаться MQTT meta/state в Nord; повторить SQL — строка setpoint есть.
5. Dry-run: `POST /api/v1/houses/house/ops/get_kettle` — `appliance.setpoint_c` число или `null` пока LM не прислал state.

Пока пункта 5 нет — HA (Task 8) **не** ставит `TARGET_TEMPERATURE` на `water_heater`. Текущую T показываем сразу.

- [ ] **Step 6: Commit** (только если пользователь просил; без секретов LM)

```bash
git add server/src/cottage_monitoring/ops/params.py server/src/cottage_monitoring/ops/catalog.py server/src/cottage_monitoring/services/agent_actions.py server/tests/unit/test_set_kettle.py server/tests/unit/test_agent_actions_helpers.py server/tests/unit/test_ops_drift.py
git commit -m "$(cat <<'EOF'
feat: extend set_kettle with setpoint_c without writing °C to cmd

EOF
)"
```

---

### Task 5: Skill, AGENTS, probe 17

**Files:**
- Modify: `skills/cottage-monitoring/SKILL.md`
- Modify: `skills/cottage-monitoring/references/openclaw-connection.md` (`16` → `17`)
- Modify: `specs/001-server-mqtt-ingestor/openclaw-cottage-agent-instructions.md` (канон AGENTS + probe 17)
- Modify: `server/tests/unit/test_ops_cli.py`

Telegram по-прежнему **спрашивает** перед выключением авто ТП. HA — явный switch, без этого промпта.

- [ ] **Step 1: Write the failing test**

```python
def test_skill_and_agents_mention_auto_heating_and_kettle_setpoint() -> None:
    skill = SKILL_MD.read_text(encoding="utf-8")
    agents = AGENTS_CANON.read_text(encoding="utf-8")
    for text in (skill, agents):
        assert "set_auto_heating" in text
        assert "setpoint_c" in text
    conn = REPO_ROOT / "skills/cottage-monitoring/references/openclaw-connection.md"
    assert "17" in conn.read_text(encoding="utf-8")
```

Таблица в SKILL.md — новые строки:

| User intent | Tool |
| «Авто полы / автоуправление отоплением» | `set_auto_heating` (`on`) — **спроси** перед выкл |
| «Нагрей чайник до 80» | `set_kettle(setpoint_c=80)` — не cmd bool |

Heating rules: пункт 1 заменить «Do not toggle without explicit user request» на «toggle only via `set_auto_heating` after explicit confirm; do not write `1/7/1` through `set_commands` unless user named the GA».

Routing ladder: авто полы → `set_auto_heating`; чайник до N °C → `set_kettle` с `setpoint_c`.

Канон AGENTS.md (блок в `openclaw-cottage-agent-instructions.md`): добавить те же две строки в semantic tools; «Не трогай авто-балансировку без явной просьбы» оставить; «нагрей чайник до N» → `set_kettle(setpoint_c=N)`.

`openclaw-connection.md`: `expect 17 house tools, including list_houses and set_auto_heating`.

Probe в инструкции: 17 tools.

- [ ] **Step 2: Run test to verify it fails**

Run: `cd server && python -m pytest tests/unit/test_ops_cli.py -v`

Expected: FAIL (`set_auto_heating` not in SKILL.md)

- [ ] **Step 3: Edit the markdown files as specified above**

- [ ] **Step 4: Run test to verify it passes**

Run: `cd server && python -m pytest tests/unit/test_ops_cli.py tests/unit/test_ops_drift.py -v`

Expected: PASS

**Ворота Nord:** все тесты Tasks 1–5 зелёные. HA-код можно писать; **контейнер HA на elion не стартовать**, пока Task 9 не пройдёт live Nord (образ, 008, probe 17).

- [ ] **Step 5: Commit** (только если пользователь просил)

```bash
git add skills/cottage-monitoring/SKILL.md skills/cottage-monitoring/references/openclaw-connection.md specs/001-server-mqtt-ingestor/openclaw-cottage-agent-instructions.md server/tests/unit/test_ops_cli.py
git commit -m "$(cat <<'EOF'
docs: route auto heating and kettle setpoint in OpenClaw skill

EOF
)"
```

---

### Task 6: HA snapshot + Nord HTTP-клиент (без пакета Home Assistant)

**Files:**
- Create: `ha/pytest.ini`
- Create: `ha/custom_components/cottage_monitoring/const.py`
- Create: `ha/custom_components/cottage_monitoring/nord_client.py`
- Create: `ha/custom_components/cottage_monitoring/snapshot.py`
- Test: `ha/tests/test_nord_client.py`
- Test: `ha/tests/test_snapshot.py`

Эти модули импортируют только stdlib. `pythonpath = custom_components` в `ha/pytest.ini`.

`const.py`:

```python
DOMAIN = "cottage_monitoring"
DEFAULT_SCAN_INTERVAL = 30
HOUSE_AREA_NAME = "Дом"
FLOOR_LABELS = {"1": "1 этаж", "2": "2 этаж", "outside": "Улица"}
```

`nord_client.py` — класс `NordClient`:

```python
class NordError(Exception):
    def __init__(self, status: int, body: str):
        self.status = status
        self.body = body

class NordClient:
    def __init__(self, base_url: str, api_key: str, house_id: str, *, transport=None):
        self.base_url = base_url.rstrip("/")
        self.api_key = api_key
        self.house_id = house_id
        self._transport = transport  # async callable(method, url, headers, json) -> tuple[int, dict|str]

    def _headers(self) -> dict[str, str]:
        return {"X-API-Key": self.api_key, "Content-Type": "application/json"}

    async def call_op(self, name: str, body: dict | None = None) -> dict:
        url = f"{self.base_url}/houses/{self.house_id}/ops/{name}"
        status, payload = await self._transport("POST", url, self._headers(), body or {})
        if status in (401, 403):
            raise NordError(status, str(payload))
        if status == 429:
            raise NordError(status, str(payload))
        if status >= 500 or status == 0:
            raise NordError(status, str(payload))
        if not isinstance(payload, dict):
            raise NordError(status, str(payload))
        return payload
```

Для юнит-тестов передавать fake transport. Боевой transport на aiohttp появится в `__init__.py` Task 7 (`async with session.post(...)`).

`snapshot.py` — чистые датаклассы + `HouseSnapshot.from_ops(...)`.

```python
@dataclass(frozen=True)
class LightItem:
    name: str
    on: bool
    area: str | None
    floor: str | None
    unique_id: str

@dataclass(frozen=True)
class ClimateZone:
    room: str
    area: str | None
    floor: str | None
    setpoint: float | None
    room_temp: float | None
    floor_temp: float | None
    relay_on: bool | None
    humidity: float | None
    unique_id: str

@dataclass(frozen=True)
class SensorItem:
    name: str
    kind: str  # air | floor | humidity | outdoor
    value: object
    area: str | None
    floor: str | None
    unique_id: str

@dataclass(frozen=True)
class KettleItem:
    name: str
    on: bool | None
    temp: float | None
    setpoint_c: float | None
    area: str | None
    floor: str | None
    unique_id: str
    has_setpoint: bool

@dataclass(frozen=True)
class HouseSnapshot:
    house_id: str
    online: bool
    last_seen: object
    auto_heating_enabled: bool
    lights: tuple[LightItem, ...]
    climates: tuple[ClimateZone, ...]
    sensors: tuple[SensorItem, ...]
    kettle: KettleItem | None
```

`unique_id = f"{house_id}:{kind}:{slug}"` где `slug` — имя, пробелы → `_`. Kind: `light`, `climate`, `relay`, `sensor_air`, `sensor_floor`, `sensor_humidity`, `sensor_outdoor`, `kettle`, плюс `house_online` / `auto_heating` на уровне дома.

`online` True **только** если `online_status == "online"` (`offline`/`unknown`/`partial` → False).

`auto_heating_enabled` привести через тот же смысл, что `_as_on` (True/1/"true").

Humidity на climate: для зоны взять sensor `kind=humidity` с тем же `area`, иначе `None`.

Kettle: `area = appliance.get("area") or "кухня"` (fallback только в HA-маппере). `has_setpoint = appliance.get("setpoint_c") is not None`.

Пропускать items со `status` ambiguous на уровне get_kettle (`appliance is None` → `kettle=None`).

- [ ] **Step 1: Write the failing tests**

`ha/tests/test_snapshot.py` — покрыть критерии спеки §13 п.1,4,5,6,8,9 (маппинг, не HTTP):

```python
from cottage_monitoring.snapshot import HouseSnapshot

OPS = {
    "get_house_status": {"online_status": "partial", "last_seen": "2026-08-28T00:00:00Z"},
    "list_lights": {
        "items": [
            {"name": "Свет - кухня", "on": True, "area": "кухня", "floor": "1"},
        ],
        "total": 1,
    },
    "get_climate": {
        "auto_heating_enabled": True,
        "zones": [
            {
                "room": "гостиная 1",
                "area": "гостиная",
                "floor": "1",
                "setpoint": 23,
                "room_temp": 21.5,
                "floor_temp": 26.0,
                "relay_on": True,
            }
        ],
    },
    "get_temperature": {
        "items": [
            {"name": "zb_sensor_fl1_living_room_temperature", "source": "air", "value": 21.5, "area": "гостиная", "floor": "1"},
            {"name": "Темп - гостиная 1", "source": "floor", "value": 26.0, "area": "гостиная", "floor": "1"},
            {"name": "weather outdoor", "source": "outdoor", "value": 12.0, "area": None, "floor": "outside"},
        ]
    },
    "get_sensors": {
        "items": [
            {"name": "zb_sensor_fl1_living_room_humidity", "value": 44, "area": "гостиная", "floor": "1"},
        ]
    },
    "get_kettle": {
        "status": "ok",
        "appliance": {
            "name": "ble_teapot_RK-M173S",
            "on": True,
            "temp": 54,
            "setpoint_c": None,
        },
    },
}


def test_partial_house_is_not_online() -> None:
    snap = HouseSnapshot.from_ops("house", OPS)
    assert snap.online is False
    assert snap.auto_heating_enabled is True


def test_areas_and_two_climates_share_guest_area() -> None:
    snap = HouseSnapshot.from_ops("house", OPS)
    assert snap.lights[0].area == "кухня"
    assert snap.lights[0].floor == "1"
    assert snap.climates[0].area == "гостиная"
    assert snap.climates[0].room == "гостиная 1"
    assert snap.climates[0].humidity == 44
    kinds = {s.kind for s in snap.sensors}
    assert {"air", "floor", "humidity", "outdoor"} <= kinds


def test_kettle_without_setpoint_hides_slider_flag() -> None:
    snap = HouseSnapshot.from_ops("house", OPS)
    assert snap.kettle is not None
    assert snap.kettle.temp == 54
    assert snap.kettle.has_setpoint is False
    assert snap.kettle.area == "кухня"


def test_unique_ids_stable() -> None:
    snap = HouseSnapshot.from_ops("house", OPS)
    assert snap.lights[0].unique_id == "house:light:свет_-_кухня"
    assert "ga" not in snap.lights[0].unique_id
    assert "/" not in snap.lights[0].unique_id
```

`slug` в `snapshot.py`: нижний регистр; пробелы и дефисы схлопываются в один `_`.

`ha/pytest.ini`:

```ini
[pytest]
pythonpath = custom_components
testpaths = tests
```

`ha/tests/test_nord_client.py`:

```python
# ha/tests/test_nord_client.py
from __future__ import annotations

import asyncio

import pytest

from cottage_monitoring.nord_client import NordClient, NordError


class Fake:
    def __init__(self, status: int, payload: object) -> None:
        self.status = status
        self.payload = payload
        self.calls: list = []

    async def __call__(self, method, url, headers, json):
        self.calls.append((method, url, headers, json))
        return self.status, self.payload


def test_call_op_posts_house_path_with_api_key() -> None:
    fake = Fake(200, {"items": []})
    client = NordClient(
        "http://127.0.0.1:8321/api/v1", "cm_secret", "house", transport=fake
    )

    async def _run() -> None:
        await client.call_op("list_lights", {})

    asyncio.run(_run())
    method, url, headers, body = fake.calls[0]
    assert method == "POST"
    assert url == "http://127.0.0.1:8321/api/v1/houses/house/ops/list_lights"
    assert headers["X-API-Key"] == "cm_secret"
    assert body == {}


def test_401_raises_nord_error() -> None:
    client = NordClient(
        "http://x/api/v1", "bad", "house", transport=Fake(401, {"detail": "no"})
    )

    async def _run() -> None:
        await client.call_op("list_lights")

    with pytest.raises(NordError) as ei:
        asyncio.run(_run())
    assert ei.value.status == 401
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd ha && python -m pytest tests -v`

Expected: FAIL (modules missing)

- [ ] **Step 3: Implement `from_ops`, `slug`, `NordClient` so tests pass**

Poll-набор (тот же, что coordinator в Task 7): `get_house_status`, `list_lights`, `get_climate`, `get_temperature`, `get_sensors` с `{"kind": "humidity"}`, `get_kettle`. `from_ops` принимает уже собранный dict этих ключей.

Не регистрировать `get_energy_status`, `discover`, `set_commands`, `set_light`, `get_heating_diagnostics`.

- [ ] **Step 4: Run test to verify it passes**

Run: `cd ha && python -m pytest tests -v`

Expected: PASS

- [ ] **Step 5: Commit** (только если пользователь просил)

```bash
git add ha/pytest.ini ha/custom_components/cottage_monitoring/const.py ha/custom_components/cottage_monitoring/nord_client.py ha/custom_components/cottage_monitoring/snapshot.py ha/tests
git commit -m "$(cat <<'EOF'
feat: add HA snapshot mapper against Nord Ops JSON

EOF
)"
```

---

### Task 7: HA integration — coordinator, Floors/Areas, HTTP transport

**Files:**
- Create: `ha/custom_components/cottage_monitoring/manifest.json`
- Create: `ha/custom_components/cottage_monitoring/__init__.py`
- Create: `ha/custom_components/cottage_monitoring/coordinator.py`
- Create: `ha/custom_components/cottage_monitoring/registry.py`
- Modify: `ha/custom_components/cottage_monitoring/nord_client.py` (aiohttp transport, вызывается только из HA)

`manifest.json`:

```json
{
  "domain": "cottage_monitoring",
  "name": "Cottage Monitoring",
  "codeowners": ["@AlexeyKorzhebin"],
  "config_flow": false,
  "documentation": "https://github.com/AlexeyKorzhebin/CottegeMonitoring",
  "iot_class": "local_polling",
  "issue_tracker": "https://github.com/AlexeyKorzhebin/CottegeMonitoring/issues",
  "requirements": [],
  "version": "0.1.0"
}
```

YAML-only (`config_flow: false`): семья не добавляет интеграцию из магазина; оператор кладёт блок в `configuration.yaml`. Платформы грузить через `discovery.async_load_platform` (док HA: YAML нового интеграционного домена, не `async_forward_entry_setups` — тот только для config entry).

```python
# ha/custom_components/cottage_monitoring/__init__.py
from datetime import timedelta

from homeassistant.const import Platform
from homeassistant.core import HomeAssistant
from homeassistant.helpers import discovery
from homeassistant.helpers.aiohttp_client import async_get_clientsession
from homeassistant.helpers.typing import ConfigType

from .const import DEFAULT_SCAN_INTERVAL, DOMAIN
from .coordinator import CottageCoordinator
from .nord_client import NordClient
from .transport import AiohttpTransport

PLATFORMS = (
    Platform.LIGHT,
    Platform.CLIMATE,
    Platform.SENSOR,
    Platform.BINARY_SENSOR,
    Platform.SWITCH,
    Platform.WATER_HEATER,
)

async def async_setup(hass: HomeAssistant, config: ConfigType) -> bool:
    conf = config.get(DOMAIN)
    if conf is None:
        return True
    session = async_get_clientsession(hass)
    client = NordClient(
        str(conf["base_url"]),
        str(conf["api_key"]),
        str(conf["house_id"]),
        transport=AiohttpTransport(session),
    )
    coordinator = CottageCoordinator(
        hass,
        client,
        update_interval=timedelta(seconds=int(conf.get("scan_interval", DEFAULT_SCAN_INTERVAL))),
    )
    await coordinator.async_refresh()
    hass.data[DOMAIN] = {"coordinator": coordinator, "config": conf}
    for platform in PLATFORMS:
        hass.async_create_task(
            discovery.async_load_platform(hass, platform, DOMAIN, {}, config)
        )
    return True
```

`CottageCoordinator(DataUpdateCoordinator[HouseSnapshot])`:

```python
async def _async_update_data(self) -> HouseSnapshot:
    try:
        ops = {
            "get_house_status": await self.client.call_op("get_house_status"),
            "list_lights": await self.client.call_op("list_lights"),
            "get_climate": await self.client.call_op("get_climate"),
            "get_temperature": await self.client.call_op("get_temperature"),
            "get_sensors": await self.client.call_op("get_sensors", {"kind": "humidity"}),
            "get_kettle": await self.client.call_op("get_kettle"),
        }
    except NordError as exc:
        raise UpdateFailed(str(exc)) from exc
    snap = HouseSnapshot.from_ops(self.client.house_id, ops)
    sync_floors_areas(self.hass, snap)
    return snap
```

401/403 → `UpdateFailed` → сущности `unavailable`. 429 → `UpdateFailed` (карточка не подтверждает write; retry следующим poll). 5xx/сеть — то же.

`registry.py` — API HA core (`helpers.area_registry` / `helpers.floor_registry`): `async_create` это `@callback` (вызывать из event loop, не `await`). Floor: `async_create(name: str, *, level: int | None = None) -> FloorEntry` (`.floor_id`). Area: `async_create(name: str, *, floor_id: str | None = None) -> AreaEntry` (`.id`). Update: `areas.async_update(area_id, floor_id=...)`. List: `async_list_floors()` / `async_list_areas()`.

```python
from homeassistant.helpers import area_registry as ar
from homeassistant.helpers import floor_registry as fr

from .const import FLOOR_LABELS, HOUSE_AREA_NAME
from .snapshot import HouseSnapshot

FLOOR_LEVELS = {"1": 1, "2": 2, "outside": 0}


def sync_floors_areas(hass, snap: HouseSnapshot) -> None:
    floors = fr.async_get(hass)
    areas = ar.async_get(hass)
    floor_ids: dict[str, str] = {}
    for key, label in FLOOR_LABELS.items():
        existing = next((f for f in floors.async_list_floors() if f.name == label), None)
        entry = existing or floors.async_create(label, level=FLOOR_LEVELS[key])
        floor_ids[key] = entry.floor_id
    home = next((a for a in areas.async_list_areas() if a.name == HOUSE_AREA_NAME), None)
    if home is None:
        areas.async_create(HOUSE_AREA_NAME)
    pairs = {
        (item.floor, item.area)
        for item in (*snap.lights, *snap.climates, *snap.sensors)
        if item.area
    }
    if snap.kettle and snap.kettle.area:
        pairs.add((snap.kettle.floor, snap.kettle.area))
    for floor_key, area_name in pairs:
        existing = next((a for a in areas.async_list_areas() if a.name == area_name), None)
        floor_id = floor_ids.get(floor_key) if floor_key else None
        if existing is None:
            areas.async_create(area_name, floor_id=floor_id)
        elif floor_id and existing.floor_id != floor_id:
            areas.async_update(existing.id, floor_id=floor_id)
```

В coordinator после `from_ops` вызывать `sync_floors_areas(self.hass, snap)` (не `await` — callback).

Назначение `area_id` сущности — в `async_added_to_hass` платформ (Task 8).

Команды: методы coordinator `async_set_lights(query: str, on: bool)`, `async_set_climate(query: str, setpoint_c: float)` **без** `force_relay`, `async_set_auto_heating(on: bool)`, `async_set_kettle(on=None, setpoint_c=None)`. После 2xx — `async_request_refresh()`. Оптимистичный UI допустим (`async_write_ha_state` сразу); истина — следующий poll. `get_command_status` не вызывать. `X-Cottage-Dry-Run` не слать из прод-конфига.

- [ ] **Step 1: Нет полноценного HA в pytest.** Контракт coordinator покрыт Task 6 (`from_ops` + `NordClient`). Здесь — файлы + ручная сверка импорта: имена `call_op` / `HouseSnapshot.from_ops` / `FLOOR_LABELS` совпадают с Task 6.

Создать файлы с кодом выше. `nord_client.py` не импортирует `homeassistant` и не импортирует `transport.py`. `ha/tests` импортируют только `nord_client` и `snapshot`.

```python
# ha/custom_components/cottage_monitoring/transport.py
import aiohttp

class AiohttpTransport:
    def __init__(self, session: aiohttp.ClientSession) -> None:
        self._session = session

    async def __call__(self, method, url, headers, json):
        timeout = aiohttp.ClientTimeout(total=20)
        async with self._session.request(
            method, url, headers=headers, json=json, timeout=timeout
        ) as resp:
            try:
                payload = await resp.json()
            except Exception:
                payload = await resp.text()
            return resp.status, payload
```

`ha/tests` не импортируют `transport.py`.

- [ ] **Step 2: Syntax check without HA**

Run: `python3 -m compileall -q ha/custom_components/cottage_monitoring/snapshot.py ha/custom_components/cottage_monitoring/nord_client.py ha/custom_components/cottage_monitoring/const.py && cd ha && python -m pytest tests -v`

Expected: PASS (старые тесты). `__init__.py` / `coordinator.py` могут не компилироваться без HA — это нормально; их проверит контейнер.

- [ ] **Step 3: Commit** (только если пользователь просил)

```bash
git add ha/custom_components/cottage_monitoring
git commit -m "$(cat <<'EOF'
feat: add HA coordinator polling Nord Ops REST

EOF
)"
```

---

### Task 8: HA platforms — light, climate, sensors, switch, water_heater

**Files:**
- Create: `ha/custom_components/cottage_monitoring/commands.py`
- Create: `ha/custom_components/cottage_monitoring/light.py`
- Create: `ha/custom_components/cottage_monitoring/climate.py`
- Create: `ha/custom_components/cottage_monitoring/sensor.py`
- Create: `ha/custom_components/cottage_monitoring/binary_sensor.py`
- Create: `ha/custom_components/cottage_monitoring/switch.py`
- Create: `ha/custom_components/cottage_monitoring/water_heater.py`
- Create: `ha/custom_components/cottage_monitoring/entity.py` (общий `CottageEntity`: unique_id, device_info дом, `async_added_to_hass` → area_id)
- Test: `ha/tests/test_commands.py` — какие тела уйдут в `call_op` (fake coordinator, без HA entity classes)

`entity.py` — тонкая база, которую платформы используют. Если импорт `CoordinatorEntity` требует HA, тесты команд **не** импортируют entity.py. Вместо этого `ha/tests/test_commands.py` тестирует функции:

```python
def lights_turn_on_body(name: str) -> tuple[str, dict]:
    return "set_lights", {"query": name, "on": True, "skip_unchanged": True}

def climate_set_temp_body(room: str, temp: float) -> tuple[str, dict]:
    return "set_climate", {"query": room, "setpoint_c": temp}

def auto_heating_body(on: bool) -> tuple[str, dict]:
    return "set_auto_heating", {"on": on}

def kettle_on_body() -> tuple[str, dict]:
    return "set_kettle", {"on": True}

def kettle_setpoint_body(c: float) -> tuple[str, dict]:
    return "set_kettle", {"setpoint_c": c}
```

Реальные entity вызывают эти хелперы (положить в `commands.py` без импорта HA). **Запрещено** передавать `force_relay` в `climate_set_temp_body`. Тест:

```python
def test_climate_body_has_no_force_relay() -> None:
    name, body = climate_set_temp_body("кухня", 22)
    assert name == "set_climate"
    assert "force_relay" not in body
    assert body == {"query": "кухня", "setpoint_c": 22}

def test_light_uses_set_lights_not_set_light() -> None:
    name, body = lights_turn_on_body("Свет - кухня")
    assert name == "set_lights"
    assert body["skip_unchanged"] is True
```

**light.py:** одна entity на `snap.lights` item. `is_on` из item. `async_turn_on/off` → `set_lights` с `query=item.name`. `unique_id` с snapshot. Area из `item.area`.

**climate.py:** HVACMode.HEAT only, `ClimateEntityFeature.TARGET_TEMPERATURE`, без COOL/FAN. `current_temperature = zone.room_temp`. `target_temperature = zone.setpoint`. `current_humidity = zone.humidity`. `hvac_action = HEATING if zone.relay_on else IDLE`. `async_set_temperature` → `set_climate` без `force_relay`, `query=zone.room` («гостиная 1», не area). Две зоны гостиной — два climate в одном area.

**binary_sensor.py:** (1) дом онлайн, area «Дом», `device_class=CONNECTIVITY`, `is_on = snap.online`, атрибут `last_seen`; (2) на каждую climate-зону — «Нагрев {room}», `is_on = bool(relay_on)`, **не** switch.

**switch.py:** только «Автоуправление полами», area «Дом», `is_on = snap.auto_heating_enabled`, turn_on/off → `set_auto_heating`. Имя в UI именно такое (спека: не путать с реле комнаты).

**sensor.py:** floor / air / humidity / outdoor из `snap.sensors`. `device_class=TEMPERATURE` или `HUMIDITY`. native unit °C / %. Outdoor area «Улица» если `floor==outside`.

**water_heater.py:** одна сущность если `snap.kettle`. `current_temperature = temp`. `is_on` / operation from `kettle.on` (state, не cmd). `async_turn_on/off` → `set_kettle(on=...)`. Если `has_setpoint`: `WaterHeaterEntityFeature.ON_OFF | TARGET_TEMPERATURE`, `min_temp=40`, `max_temp=100`, `async_set_temperature` → `set_kettle(setpoint_c=...)`. Если нет — только `ON_OFF`, **не** рисовать target. Отдельный `switch` чайника не создавать.

`unique_id` без GA. `entity_registry.async_update_entity(..., area_id=...)` в `async_added_to_hass`.

Платформы: `async_setup_platform(hass, config, async_add_entities, discovery_info=None)` читает `hass.data[DOMAIN]["coordinator"]` и добавляет список entity.

- [ ] **Step 1: Write failing tests for `commands.py`**

- [ ] **Step 2: Fail** (`commands` missing)

- [ ] **Step 3: Implement `commands.py` + all platform modules**

Скелет light (остальные по тому же coordinator pattern):

```python
async def async_setup_platform(hass, config, async_add_entities, discovery_info=None):
    coordinator = hass.data[DOMAIN]["coordinator"]
    async_add_entities(
        [CottageLight(coordinator, item) for item in coordinator.data.lights],
        True,
    )

class CottageLight(CoordinatorEntity, LightEntity):
    _attr_has_entity_name = True

    def __init__(self, coordinator, item: LightItem):
        super().__init__(coordinator)
        self._item_name = item.name
        self._attr_unique_id = item.unique_id
        self._attr_name = item.name
        self._area = item.area
        self._floor = item.floor

    @property
    def is_on(self) -> bool:
        match = next(i for i in self.coordinator.data.lights if i.name == self._item_name)
        return match.on

    async def async_turn_on(self, **kwargs):
        op, body = lights_turn_on_body(self._item_name)
        await self.coordinator.client.call_op(op, body)
        await self.coordinator.async_request_refresh()
```

`async_setup_platform` для climate/sensors должен **пересоздавать** список на каждом refresh или использовать уникальные id и `async_add_entities` только для новых. Минимально: на setup взять текущий snapshot; новые объекты появятся после reload интеграции. Для v1 этого достаточно (схема дома стабильна). Комментарий в `__init__.py`: смена состава зон → Settings → reload YAML / restart HA.

- [ ] **Step 4: Run**

Run: `cd ha && python -m pytest tests -v`

Expected: PASS (`test_commands`, snapshot, client)

- [ ] **Step 5: Commit** (только если пользователь просил)

```bash
git add ha/custom_components/cottage_monitoring ha/tests
git commit -m "$(cat <<'EOF'
feat: map Nord Ops to HA lights, climate, sensors and kettle

EOF
)"
```

---

### Task 9: systemd, nginx, volume, sync-скрипт, канон configuration.yaml

**Files:**
- Create: `server/deploy/home-assistant.service`
- Create: `server/deploy/nginx/home-assistant.conf`
- Create: `server/deploy/ha-sync-component.sh`
- Create: `server/deploy/ha/configuration.yaml`
- Create: `server/deploy/ha/secrets.yaml.example`
- Modify: `server/pyproject.toml` (`version = "0.3.0"`)
- Modify: `server/deploy/IMAGE_PIN.yaml`
- Modify: `server/deploy/cottage-monitoring.service` (тег `cottage-monitoring:0.3.0`)

`home-assistant.service`:

```ini
[Unit]
Description=Home Assistant Container (family UI on Nord)
After=network.target docker.service cottage-monitoring.service
Requires=docker.service

[Service]
Type=simple
Restart=always
RestartSec=5
ExecStartPre=-/usr/bin/docker stop home-assistant
ExecStartPre=-/usr/bin/docker rm home-assistant
ExecStart=/usr/bin/docker run --name home-assistant \
  --network host \
  -v /var/lib/homeassistant:/config \
  ghcr.io/home-assistant/home-assistant:stable
ExecStop=/usr/bin/docker stop home-assistant

[Install]
WantedBy=multi-user.target
```

Официальный образ pull на elion **допустим** (это не наш код).

`server/deploy/ha/configuration.yaml` (канон, кладётся в volume **один раз**, секреты не из git):

```yaml
default_config:

http:
  server_host: 127.0.0.1
  server_port: 8123
  use_x_forwarded_for: true
  trusted_proxies:
    - 127.0.0.1

automation: !include automations.yaml

cottage_monitoring:
  base_url: http://127.0.0.1:8321/api/v1
  api_key: !secret nord_ha_api_key
  house_id: house
  scan_interval: 30
```

Рядом в каноне `server/deploy/ha/automations.yaml` с единственной строкой `[]`.

`secrets.yaml.example`:

```yaml
nord_ha_api_key: cm_REPLACE_ME
```

`nginx/home-assistant.conf` — отдельный `server`, **не** location на monitoring.black-castle.ru:

```nginx
server {
    listen 80;
    server_name ha.black-castle.ru;
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name ha.black-castle.ru;
    # ssl_certificate paths: certbot впишет при --nginx

    location / {
        proxy_pass http://127.0.0.1:8123;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400s;
        proxy_buffering off;
    }
}
```

Не добавлять `location /mcp` на этот server.

`ha-sync-component.sh` (из корня репо, не git clone на elion):

```bash
#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
# script lives in server/deploy → repo root is ../..
tar -C "$ROOT/ha/custom_components" -czf - cottage_monitoring \
  | ssh elion 'sudo mkdir -p /var/lib/homeassistant/custom_components \
    && sudo tar -C /var/lib/homeassistant/custom_components -xzf - \
    && sudo find /var/lib/homeassistant/custom_components/cottage_monitoring -type d -exec chmod 755 {} \; \
    && sudo find /var/lib/homeassistant/custom_components/cottage_monitoring -type f -exec chmod 644 {} \;'
```

Путь ROOT: файл `server/deploy/ha-sync-component.sh` → `$(dirname "$0")/../..` это корень репо. Проверить `test -d "$ROOT/ha/custom_components/cottage_monitoring"`.

- [ ] **Step 1: Файлы существуют; shellcheck не обязателен.** Проверка: `test -f server/deploy/home-assistant.service` и в nginx **нет** `location /mcp`.

```bash
# из корня
grep -n mcp server/deploy/nginx/home-assistant.conf && exit 1 || true
grep -n 'server_host: 127.0.0.1' server/deploy/ha/configuration.yaml
grep -n '0.3.0' server/pyproject.toml server/deploy/IMAGE_PIN.yaml server/deploy/cottage-monitoring.service
```

Expected: grep mcp → no match (exit 0 from `|| true` after `&& exit 1` not triggered); version 0.3.0 во всех трёх.

`IMAGE_PIN.yaml`:

```yaml
version: "0.3.0"
git_ref: "main"
image_tag: "cottage-monitoring:0.3.0"
image_digest: ""
overlay_retired: true
replaces_overlay: "cottage-monitoring:0.2.9"
notes: |
  v0.3.0: HA-facing Ops — area/floor on reads, set_auto_heating, set_kettle setpoint_c.
```

- [ ] **Step 2: Commit** (только если пользователь просил; `secrets.yaml` боевой не класть)

```bash
git add server/deploy/home-assistant.service server/deploy/nginx/home-assistant.conf server/deploy/ha-sync-component.sh server/deploy/ha server/deploy/IMAGE_PIN.yaml server/deploy/cottage-monitoring.service server/pyproject.toml
git commit -m "$(cat <<'EOF'
chore: pin Nord 0.3.0 and add HA container deploy units

EOF
)"
```

---

### Task 10: Спеки + live elion (Nord сначала, HA потом)

**Files:**
- Modify: `specs/001-server-mqtt-ingestor/research.md` — **R-024**, **R-025** + строки в таблице в конце
- Modify: `specs/001-server-mqtt-ingestor/quickstart.md` — pin 0.3.0, каталог 17, HA, alembic 008 до рестарта
- Modify: `docs/superpowers/specs/2026-08-28-ha-nord-design.md` — Status: `Ready for deploy` после Tasks 1–9; `Implemented` после live smoke Task 10 Step 3

**R-024 (2026-08-28): Placement + set_auto_heating + kettle setpoint**

Решение: `area`/`floor` на существующих read-Ops; Op `set_auto_heating` → `1/7/1`; `set_kettle` принимает `on` и/или `setpoint_c` 40–100; °C не пишется в cmd bool. Каталог 17 имён. Объект уставки чайника заводится на LM (имя `ble_teapot_RK-M173S_setpoint`), иначе HA без слайдера.

**R-025 (2026-08-28): HA Container на REST-грани**

Решение: официальный Container, `--network host`, `http.server_host: 127.0.0.1:8123`, nginx `ha.black-castle.ru` + WebSocket, ключ `home-assistant`, custom component из `ha/custom_components/cottage_monitoring/`. Нет KNX/MQTT/`cm/#`, нет домашних automation. Семья — users HA; Nord видит один `actor_key_id`.

Quickstart: заменить «16 операций» на **17**; probe 17; порядок migrate→restart; секция HA со ссылкой на unit/nginx/sync; `ss -lntp | grep 8123` должен показать **127.0.0.1:8123**, не `0.0.0.0`.

- [ ] **Step 1: Написать R-024/R-025 и поправить quickstart** (факт после кода Tasks 1–9; live-статус 008 — «накатить на prod в шагах ниже»).

- [ ] **Step 2: Live Nord — строго этот порядок**

С локальной машины (из `server/`):

```bash
# 1. образ
docker build --platform linux/amd64 -t cottage-monitoring:0.3.0 -f deploy/Dockerfile .
docker save cottage-monitoring:0.3.0 | ssh elion 'sudo docker load'

# 2. alembic 008 на PROD до рестарта (контейнер 0.2.9 ещё работает)
ssh elion 'sudo docker run --rm --network=host \
  --env-file /etc/cottage-monitoring/cottage-monitoring.prod.env \
  cottage-monitoring:0.3.0 alembic upgrade head'

# 3. unit pin уже в git; скопировать unit на elion тем же операторским путём, что для прошлых pin
# (не git clone продукта). Затем:
ssh elion 'sudo systemctl daemon-reload && sudo systemctl restart cottage-monitoring'
ssh elion '/opt/cottage-monitoring/wait_http_health.sh http://127.0.0.1:8321/health 30'

# 4. skill
# скопировать skills/cottage-monitoring на elion в workspace skills (openclaw skills install … --force)
ssh elion 'sudo -u openclaw openclaw mcp probe cottage'
# expect 17 tools including list_houses and set_auto_heating
# старый Telegram: /new
```

Ключ HA:

```bash
ssh elion 'sudo docker run --rm --network=host \
  --env-file /etc/cottage-monitoring/cottage-monitoring.prod.env \
  --entrypoint cottage-create-api-key cottage-monitoring:0.3.0 \
  --house house --name home-assistant --scopes read,write'
# секрет один раз → /var/lib/homeassistant/secrets.yaml nord_ha_api_key
# не в git
```

Smoke (с elion, loopback):

```bash
curl -sS -H "X-API-Key: $HA_KEY" http://127.0.0.1:8321/api/v1/ops | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d["total"], sorted(i["name"] for i in d["items"]))'
# 17 имён, есть set_auto_heating

curl -sS -X POST -H "X-API-Key: $HA_KEY" -H "Content-Type: application/json" \
  http://127.0.0.1:8321/api/v1/houses/house/ops/list_lights -d '{}'
# items[].area и items[].floor присутствуют

curl -sS -X POST -H "X-API-Key: $HA_KEY" -H "X-Cottage-Dry-Run: 1" -H "Content-Type: application/json" \
  http://127.0.0.1:8321/api/v1/houses/house/ops/get_climate -d '{}'
# 2xx, auto_heating_enabled, zones[].area
```

Пока это не зелёное — **не** стартовать HA.

- [ ] **Step 3: Live HA**

1. DNS: A `ha.black-castle.ru` = тот же IP, что `monitoring.black-castle.ru`. Проверка: `dig +short ha.black-castle.ru` == `dig +short monitoring.black-castle.ru`.
2. Volume: `sudo mkdir -p /var/lib/homeassistant`; положить `configuration.yaml` + пустой `automations.yaml` + `secrets.yaml` (ключ).
3. `./server/deploy/ha-sync-component.sh`
4. nginx site + `sudo certbot --nginx -d ha.black-castle.ru`
5. `sudo systemctl enable --now home-assistant`
6. `ss -lntp | grep 8123` → только `127.0.0.1:8123`
7. Onboarding владельца на `https://ha.black-castle.ru`; Settings → People — по логину на человека; обычные без админа.
8. Reload YAML / restart HA после появления компонента.
9. Smoke UI: этажи и комнаты; в комнате свет + climate + воздух + влажность + пол + binary_sensor реле (не switch); «Дом» — онлайн и авто полы; кухня — water_heater, текущая T; слайдер уставки только если `get_kettle.setpoint_c` не null.
10. Grafana по-прежнему SELECT; в HA нет интеграции KNX/MQTT.

- [ ] **Step 4: Commit спек** (только если пользователь просил)

```bash
git add specs/001-server-mqtt-ingestor/research.md specs/001-server-mqtt-ingestor/quickstart.md docs/superpowers/specs/2026-08-28-ha-nord-design.md
git commit -m "$(cat <<'EOF'
docs: record HA-on-Nord ops notes R-024 and R-025

EOF
)"
```

---

## Self-review vs spec

| Спека | Task |
|-------|------|
| Сначала живой Nord Ops (008 до restart, skill, probe 17) | 10 (после 1–5) |
| `area`/`floor` на read-Ops, Zigbee маппинг в Nord | 1, 2 |
| `set_auto_heating` → `1/7/1` | 3 |
| Чайник `water_heater`, T сейчас, setpoint без записи в cmd bool; объект на LM | 4, 8 |
| Каталог 17, skill/AGENTS | 3, 5 |
| REST-грань, ключ home-assistant, не MCP публично | 6, 9, 10 |
| HA Container host network, loopback 8123, nginx WS | 9, 10 |
| Floors/Areas из Ops; area «Дом» | 6, 7, 8 |
| binary_sensor реле; climate без force_relay; авто ТП switch | 8 |
| Poll 30 с: status, lights, climate, temperature, humidity, kettle | 7 |
| 401 → unavailable | 6, 7 |
| Grafana SELECT; нет KNX/MQTT/automation дома | 9, 10 constraints |
| Тесты §13.1–11 | 1–6, 8 (`commands`), 10 live |
| Non-goals (Grafana iframe, энергия, батареи, MQTT discovery, users Nord, TOOLS.md, SDK 2.x) | не в file map |

Пробелов по goals спеки нет. Placeholder-скан: нет TBD/TODO в шагах; LM setpoint — конкретные SQL + шаги Mosaic, не «разобраться позже».

---

## Как сопряжены грани

Telegram (MCP) и HA (REST) вызывают одни handlers. После выкладки ключ HA пишет `commands.actor_key_id` отдельно от ключа OpenClaw. Семья кликает в HA — Nord видит только `home-assistant`.
